# Story 3.3 — Página Detalhe da OP

**Epic:** 3 — Frontend (Telas)
**Story:** 3.3
**Status:** Done
**Criado por:** @sm (River)
**Data:** 2026-06-30

---

## Story

**Como** operador, supervisor ou gerente,
**Quero** visualizar o resumo completo de uma ordem de produção e a lista de couros processados nela,
**Para que** eu possa analisar o desempenho da OP e acessar o detalhe de cada couro individualmente.

---

## Acceptance Criteria

- [x] AC-01: A página `/ops/:id` exibe resumo agregado da OP: número, total de couros, área média, aproveitamento médio
- [x] AC-02: A página exibe gráfico de distribuição de classes da OP (usando `ClassDistributionChart` criado na Story 3.1)
- [x] AC-03: A página exibe lista paginada dos couros da OP com colunas: Número do Couro, Data/Hora, Classe, Aproveitamento, Área
- [x] AC-04: Cada couro na lista tem link clicável para `/ops/:opId/hides/:hideId`
- [x] AC-05: A lista de couros usa paginação com botões anterior/próximo (usando `Pagination` criado na Story 3.2)
- [x] AC-06: Os dados do resumo são carregados via `GET /ops/{id}` e os couros via `GET /ops/{id}/hides`
- [x] AC-07: Durante o carregamento, exibe indicador de loading
- [x] AC-08: Se a OP não for encontrada (API retorna 404), exibe mensagem "OP não encontrada" com link para voltar à lista
- [x] AC-09: Nenhuma imagem é exibida nesta tela (conforme PRD seção 3.3)
- [x] AC-10: `npm run build` passa sem erros de TypeScript após a implementação

---

## Scope

### IN
- Componente `src/pages/OpDetail.tsx` (substituir placeholder)
- Componente `src/components/tables/HideTable.tsx`
- Chamadas à API `GET /ops/{id}` e `GET /ops/{id}/hides`
- Reutilização de `ClassDistributionChart` (Story 3.1) e `Pagination` (Story 3.2)
- Tipos TypeScript para `OpDetail` e `HideListResponse` em `src/types/op.ts` e `src/types/hide.ts`

### OUT
- Botões de exportação PDF/Excel (Epic 4)
- Filtros na lista de couros desta tela
- Imagens de couros

---

## Dependencies

- **Story 2.3** concluída (`GET /ops/{id}` e `GET /ops/{id}/hides` disponíveis)
- **Story 3.1** concluída (`ClassDistributionChart` disponível)
- **Story 3.2** concluída (`Pagination` e `HideTable` base disponíveis)

---

## Dev Notes

### Contratos da API
[Source: docs/architecture/system-architecture.md#4.3-detalhe-da-op]
[Source: docs/architecture/system-architecture.md#4.4-couros-de-uma-op]

**GET /ops/{op_id}:**
```json
{
  "op": "12345",
  "total_hides": 48,
  "avg_area": 4.32,
  "avg_yield": 79.2,
  "class_distribution": [
    { "class": "A", "count": 28, "pct": 58.3 },
    { "class": "B", "count": 20, "pct": 41.7 }
  ]
}
```

**GET /ops/{op_id}/hides?page=1&limit=50:**
```json
{
  "items": [
    {
      "id": 1001,
      "hide_num": "H001",
      "processed_at": "2026-06-26T14:32:00",
      "class": "A",
      "yield_pct": 81.2,
      "area": 4.5,
      "op": "12345"
    }
  ],
  "total": 48,
  "page": 1,
  "limit": 50
}
```

### Tipos TypeScript a Adicionar

**Em `src/types/op.ts` — adicionar:**
```typescript
export interface OpDetail {
  op: string;
  total_hides: number;
  avg_area: number | null;
  avg_yield: number | null;
  class_distribution: ClassDistributionItem[]; // já definido na Story 3.1
}
```

**Em `src/types/hide.ts` — adicionar:**
```typescript
export interface HideItem {
  id: number;
  hide_num: string;
  processed_at: string;
  class: string;
  yield_pct: number | null;
  area: number | null;
  op: string;
}

export interface HideListResponse {
  items: HideItem[];
  total: number;
  page: number;
  limit: number;
}
```

### Lógica do Componente OpDetail

```typescript
// src/pages/OpDetail.tsx
import { useEffect, useState } from 'react';
import { useParams, Link } from 'react-router-dom';
import client from '../api/client';
import { OpDetail as OpDetailType } from '../types/op';
import { HideListResponse } from '../types/hide';
import ClassDistributionChart from '../components/charts/ClassDistributionChart';
import HideTable from '../components/tables/HideTable';
import Pagination from '../components/ui/Pagination';

export default function OpDetail() {
  const { id } = useParams<{ id: string }>();
  const [opData, setOpData] = useState<OpDetailType | null>(null);
  const [hidesData, setHidesData] = useState<HideListResponse | null>(null);
  const [page, setPage] = useState(1);
  const [loading, setLoading] = useState(true);
  const [notFound, setNotFound] = useState(false);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    setLoading(true);
    Promise.all([
      client.get<OpDetailType>(`/ops/${id}`),
      client.get<HideListResponse>(`/ops/${id}/hides`, { params: { page, limit: 50 } }),
    ])
      .then(([opRes, hidesRes]) => {
        setOpData(opRes.data);
        setHidesData(hidesRes.data);
      })
      .catch(err => {
        if (err.response?.status === 404) setNotFound(true);
        else setError('Erro ao carregar dados da OP');
      })
      .finally(() => setLoading(false));
  }, [id, page]);

  if (loading) return <div>Carregando...</div>;
  if (notFound) return <div>OP não encontrada. <Link to="/ops">Voltar à lista</Link></div>;
  if (error) return <div>{error}</div>;
  if (!opData || !hidesData) return null;

  return (
    <div>
      {/* Resumo da OP */}
      {/* ClassDistributionChart com class_distribution */}
      {/* HideTable com items */}
      {/* Pagination */}
    </div>
  );
}
```

> **Nota:** Ao trocar de página (`setPage`), apenas `GET /ops/{id}/hides` deve ser recarregado — não o resumo da OP. Refatorar o `useEffect` em dois separados: um para o resumo (depende só de `id`) e outro para os couros (depende de `id` e `page`).

### Componente HideTable

```typescript
// src/components/tables/HideTable.tsx
import { Link } from 'react-router-dom';
import { HideItem } from '../../types/hide';

interface Props {
  items: HideItem[];
  opId: string;
}

export default function HideTable({ items, opId }: Props) {
  if (items.length === 0) return <p>Nenhum couro encontrado.</p>;

  return (
    <table>
      <thead>
        <tr>
          <th>Couro</th>
          <th>Data/Hora</th>
          <th>Classe</th>
          <th>Aproveitamento</th>
          <th>Área (m²)</th>
        </tr>
      </thead>
      <tbody>
        {items.map(hide => (
          <tr key={hide.id}>
            <td>
              <Link to={`/ops/${opId}/hides/${hide.id}`}>{hide.hide_num}</Link>
            </td>
            <td>{new Date(hide.processed_at).toLocaleString('pt-BR')}</td>
            <td>{hide.class}</td>
            <td>{hide.yield_pct?.toFixed(1) ?? '—'}%</td>
            <td>{hide.area?.toFixed(2) ?? '—'}</td>
          </tr>
        ))}
      </tbody>
    </table>
  );
}
```

### Rota no App.tsx

A rota `/ops/:id` já deve estar configurada em `App.tsx` (Story 1.3). Verificar que `OpDetail` é o componente correto associado a ela.

### Arquivos Relevantes
[Source: docs/architecture/system-architecture.md#3-estrutura-de-pastas]

```
lizard-web/frontend/src/
├── pages/OpDetail.tsx              ← substituir placeholder
├── components/tables/HideTable.tsx ← criar
├── types/op.ts                     ← adicionar OpDetail
├── types/hide.ts                   ← adicionar HideItem, HideListResponse
├── components/charts/ClassDistributionChart.tsx ← já existe (Story 3.1), reutilizar
└── components/ui/Pagination.tsx    ← já existe (Story 3.2), reutilizar
```

---

## Tasks / Subtasks

- [x] **Task 1** — Adicionar tipo `OpDetail` em `src/types/op.ts` (AC-01, AC-02)
- [x] **Task 2** — Adicionar tipos `HideItem` e `HideListResponse` em `src/types/hide.ts` (AC-03)
- [x] **Task 3** — Criar `src/components/tables/HideTable.tsx` com colunas e link para detalhe do couro (AC-03, AC-04)
- [x] **Task 4** — Implementar `src/pages/OpDetail.tsx` com dois `useEffect` separados: um para o resumo (`GET /ops/{id}`), outro para os couros (`GET /ops/{id}/hides`) (AC-01, AC-06, AC-07)
- [x] **Task 5** — Exibir resumo agregado: número da OP, total de couros, área média, aproveitamento médio (AC-01)
- [x] **Task 6** — Renderizar `ClassDistributionChart` com `class_distribution` (AC-02)
- [x] **Task 7** — Renderizar `HideTable` com `items` e `Pagination` para troca de página dos couros (AC-03, AC-05)
- [x] **Task 8** — Tratar 404 da API com mensagem e link de volta à lista (AC-08)
- [x] **Task 9** — Executar `npm run build` e corrigir erros de TypeScript (AC-10)
- [ ] **Task 10** — Testar no browser: dados corretos exibidos, paginação dos couros, link de couro navega para detalhe, 404 exibe mensagem com link de retorno (AC-01 a AC-09)

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Frontend
- Secondary Type: —
- Complexity: Medium — duas chamadas à API paralelas, paginação independente, reutilização de componentes, tratamento de 404

**Specialized Agent Assignment:**
- Primary Agents: @dev
- Supporting Agents: nenhum necessário

**Quality Gate Tasks:**
- [x] Pre-Commit (@dev): Verificar dois `useEffect` separados (resumo vs couros), tipagem sem `any`, 404 tratado corretamente, `npm run build` sem erros
- [ ] Pre-PR (@devops): Verificar no browser navegação completa antes do PR

**CodeRabbit Focus Areas:**
- React: `useEffect` separados para evitar re-fetch desnecessário do resumo ao paginar
- TypeScript: tipos `null` para `avg_area`/`avg_yield` opcionais
- UX: 404 com mensagem clara e link de retorno, loading durante fetch
- Reutilização: `ClassDistributionChart` e `Pagination` importados corretamente

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Severity Filter: CRITICAL
- CRITICAL: auto_fix; HIGH: document_only

---

## File List

- `lizard-web/frontend/src/types/hide.ts` — adicionados `HideItem` e `HideListResponse`
- `lizard-web/frontend/src/components/tables/HideTable.tsx` — criado com 5 colunas e link para detalhe do couro
- `lizard-web/frontend/src/pages/OpDetail.tsx` — implementado com dois useEffect separados, resumo, gráfico, tabela de couros e paginação independente

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-06-30 | @sm (River) | Story criada — Status: Draft |
| 2026-06-30 | @po (Pax) | Validação GO (10/10) — Status: Draft → Ready |
| 2026-06-30 | @dev (Dex) | Implementação completa, npm run build OK — Status: Ready → InReview |
| 2026-06-30 | @qa (Quinn) | QA Gate PASS — todos os 10 ACs verificados, build TypeScript OK — Status: InReview → Done |

---

*Story 3.3 — Lizard Web — @sm (River) — 2026-06-30*
