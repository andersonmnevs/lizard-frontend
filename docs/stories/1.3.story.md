# Story 1.3 — Setup React + Vite com Roteamento e Layout Base

**Epic:** 1 — Infraestrutura Base
**Story:** 1.3
**Status:** Ready
**Criado por:** @sm (River)
**Data:** 2026-06-26

---

## Story

**Como** desenvolvedor do projeto Lizard Web,
**Quero** ter o frontend React configurado com roteamento entre as 4 páginas e um layout base com navegação,
**Para que** as stories do Epic 3 possam implementar cada página sobre uma estrutura de navegação funcional.

---

## Acceptance Criteria

- [ ] AC-01: `src/App.tsx` configura `react-router-dom` com rotas para as 4 páginas: `/` (Dashboard), `/ops` (Lista de OPs), `/ops/:id` (Detalhe da OP), `/ops/:opId/hides/:hideId` (Detalhe do Couro)
- [ ] AC-02: Componente de layout base `src/components/ui/Layout.tsx` renderiza header com nome da aplicação ("Lizard Web") e navegação com links para Dashboard e Lista de OPs
- [ ] AC-03: Cada uma das 4 páginas (`Dashboard.tsx`, `OpList.tsx`, `OpDetail.tsx`, `HideDetail.tsx`) existe em `src/pages/` e renderiza um placeholder com o nome da página
- [ ] AC-04: `src/api/client.ts` exporta instância Axios configurada com `baseURL` apontando para `http://localhost:8000` (ou relativo `/` quando em produção)
- [ ] AC-05: `src/types/op.ts` e `src/types/hide.ts` exportam interfaces TypeScript correspondendo aos contratos de response da API definidos na arquitetura
- [ ] AC-06: `npm run dev` inicia o servidor de desenvolvimento sem erros e exibe as páginas placeholder acessíveis via browser
- [ ] AC-07: `npm run build` gera o bundle estático em `frontend/dist/` sem erros de TypeScript
- [ ] AC-08: Navegação entre as 4 páginas funciona via links do header e botão voltar do browser

---

## Scope

### IN
- Configuração de rotas com `react-router-dom`
- Componente Layout com header e navegação
- 4 páginas placeholder (apenas estrutura, sem dados reais)
- Cliente Axios configurado (`src/api/client.ts`)
- Interfaces TypeScript para OP e Couro
- Build de produção funcional

### OUT
- Implementação de dados reais em qualquer página (Epic 3)
- Chamadas à API (Epic 3)
- Gráficos e tabelas (Epic 3)
- Visualizador de imagens (Epic 3)
- Estilização avançada além do layout base (Epic 3)

---

## Dependencies

- **Story 1.1** deve estar concluída (projeto React inicializado, dependências instaladas)
- **Story 1.2** deve estar concluída (API rodando para teste do proxy)

---

## Dev Notes

### Estrutura de Rotas
[Source: docs/architecture/system-architecture.md#3-estrutura-de-pastas e #4-api-contratos-de-endpoints]

```tsx
// App.tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import Layout from './components/ui/Layout'
import Dashboard from './pages/Dashboard'
import OpList from './pages/OpList'
import OpDetail from './pages/OpDetail'
import HideDetail from './pages/HideDetail'

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Layout />}>
          <Route index element={<Dashboard />} />
          <Route path="ops" element={<OpList />} />
          <Route path="ops/:id" element={<OpDetail />} />
          <Route path="ops/:opId/hides/:hideId" element={<HideDetail />} />
        </Route>
      </Routes>
    </BrowserRouter>
  )
}
```

### Cliente Axios
[Source: docs/architecture/system-architecture.md#2-stack-tecnológica]

```typescript
// src/api/client.ts
import axios from 'axios'

const client = axios.create({
  baseURL: import.meta.env.VITE_API_URL || 'http://localhost:8000',
  headers: { 'Content-Type': 'application/json' }
})

export default client
```

Em desenvolvimento, o proxy do Vite redireciona `/api` para `http://localhost:8000`. Em produção, a API e o frontend rodam na mesma origem (FastAPI serve o `dist/`).

### Interfaces TypeScript
[Source: docs/architecture/system-architecture.md#4-api-contratos-de-endpoints]

```typescript
// src/types/op.ts
export interface OpSummary {
  op: string
  last_updated: string
  total_hides: number
  avg_yield: number | null
  predominant_class: string | null
}

export interface OpDetail {
  op: string
  total_hides: number
  avg_area: number | null
  avg_yield: number | null
  class_distribution: ClassDistribution[]
}

export interface ClassDistribution {
  class: string
  count: number
  pct: number
}

// src/types/hide.ts
export interface Hide {
  id: number
  hide_num: string
  op: string
  processed_at: string
  class: string
  yield_pct: number | null
  area: number | null
  image_available: boolean
  prev_hide_id: number | null
  next_hide_id: number | null
}
```

### Layout Base
O header deve conter:
- Nome da aplicação: "Lizard Web"
- Link para Dashboard (`/`)
- Link para Lista de OPs (`/ops`)

Usar `<Outlet />` do `react-router-dom` para renderizar a página filha dentro do Layout.

### Variáveis de Ambiente Vite
Criar `lizard-web/frontend/.env.example`:
```
VITE_API_URL=http://localhost:8000
```

### Build de Produção
`npm run build` gera `frontend/dist/`. Na Story 1.2 (ou 2.x), o FastAPI será configurado para servir este diretório como arquivos estáticos.

---

## Tasks / Subtasks

- [ ] **Task 1** — Criar interfaces TypeScript em `src/types/op.ts` e `src/types/hide.ts` baseadas nos contratos da arquitetura (AC-05)
- [ ] **Task 2** — Criar `src/api/client.ts` com instância Axios configurada (AC-04)
- [ ] **Task 3** — Criar 4 páginas placeholder em `src/pages/`: `Dashboard.tsx`, `OpList.tsx`, `OpDetail.tsx`, `HideDetail.tsx` (AC-03)
- [ ] **Task 4** — Criar `src/components/ui/Layout.tsx` com header, links de navegação e `<Outlet />` (AC-02)
- [ ] **Task 5** — Configurar roteamento em `src/App.tsx` com as 4 rotas usando Layout como wrapper (AC-01)
- [ ] **Task 6** — Criar `frontend/.env.example` com `VITE_API_URL` (AC-04)
- [ ] **Task 7** — Verificar `npm run dev` inicia sem erros e páginas placeholder são acessíveis (AC-06)
- [ ] **Task 8** — Verificar `npm run build` gera `dist/` sem erros TypeScript (AC-07)
- [ ] **Task 9** — Testar navegação entre todas as páginas via links e botão voltar do browser (AC-08)

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Frontend
- Secondary Type: Architecture
- Complexity: Low — estrutura de roteamento e placeholders, sem lógica de dados

**Specialized Agent Assignment:**
- Primary Agents: @dev
- Supporting Agents: nenhum necessário para esta story

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Verificar build TypeScript sem erros e roteamento funcional
- [ ] Pre-PR (@devops): Verificar estrutura de componentes antes do PR

**CodeRabbit Focus Areas:**
- TypeScript: interfaces alinhadas com contratos da API
- Roteamento: todas as 4 rotas configuradas corretamente
- Isolamento: frontend não acessa `lizard/` diretamente
- Build: `npm run build` sem erros

**Self-Healing Configuration:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Severity Filter: CRITICAL

---

## File List

*Preenchido pelo @dev durante implementação*

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-06-26 | @sm (River) | Story criada — Status: Draft |
| 2026-06-26 | @po (Pax) | Validação GO 8/10 — Status: Draft → Ready. Obs: dependência de Story 1.2 para teste do proxy é correta mas pode ser relaxada — proxy pode ser testado manualmente |

---

*Story 1.3 — Lizard Web — @sm (River) — 2026-06-26*
